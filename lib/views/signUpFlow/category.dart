import 'package:flutter/material.dart';

class CategoryNames extends StatelessWidget {
  const CategoryNames({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}